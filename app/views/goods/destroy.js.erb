$("#good_form-<%= @micropost.reload.id %>").html("<%= escape_javascript(render('microposts/good')) %>");
if (<%= @user_id == current_user.id %>) {
  $("#good_num").html("<%= Good.where(created_by_id: current_user.id).count %>");
}
